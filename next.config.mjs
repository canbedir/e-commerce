/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["firebasestorage.googleapis.com", "getmobil.com", "resource.logitech.com", "www.kvk.com", "www.pngall.com"],
  },
};

export default nextConfig;
